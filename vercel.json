{
    "builds": [
      {
        "src": "backend/server.js",
        "use": "@vercel/node",
        "config": {
          "maxLambdaSize": "50mb"
        },
        "buildCommand": "npm run backend"
      }
    ]
  }